/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "mye-website.local",
      "mye-wordpress-dev.amihan.net",
      "secure.gravatar.com",
    ],
  },
};

module.exports = nextConfig;
